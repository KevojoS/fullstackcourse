{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "buildui": "rm -rf dist && cd frontend/ && npm run build && cp -r dist ../ && rm -rf dist",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "git add . && git commit -m buildui && git push"
  },
  "author": "Kevin Sa",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "mongoose": "^8.18.0"
  }
}
